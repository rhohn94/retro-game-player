/// <reference path="./jsx.d.ts" />
/* ==========================================================================
   Aura — React bindings — TypeScript definitions.
   GENERATED by tools/aura_react.py from the element registry (js/*.js).
      Do not edit by hand — edit the generator and rerun `just react`.
   ========================================================================== */
import {
  ForwardRefExoticComponent,
  HTMLAttributes,
  PropsWithChildren,
  RefAttributes,
} from "react";

/* --------------------------------------------------------------------------
 * Custom-event detail payloads (from the actual dispatch sites in js/)
 * ----------------------------------------------------------------------- */

/** Detail for `aura:change` — the shared value-change event. Which fields are present depends on the dispatching element. */
export interface AuraChangeDetail {
  /** New value (select / range / pickers / stepper / color / checkbox / radio). */
  value?: string;
  /** Display label of the selection (select / datepicker). */
  label?: string;
  /** New checked state (aura-switch / aura-checkbox / aura-radio). */
  checked?: boolean;
  /** Post-toggle mixed state (aura-checkbox / aura-radio). */
  indeterminate?: boolean;
  /** Current tag list (aura-tag-input). */
  tags?: string[];
  /** Panel ratios after a resize (aura-split). */
  ratios?: number[];
  /** Selected date (aura-datepicker). */
  date?: Date | null;
  /** Range start (datepicker / timepicker range mode). */
  start?: string | Date | null;
  /** Range end (datepicker / timepicker range mode). */
  end?: string | Date | null;
  /** Both range ends committed (datepicker / timepicker range mode). */
  complete?: boolean;
  /** True for multi-select change events (aura-select). */
  multiple?: boolean;
  /** RGB triple from aura-color-picker (r/g/b each 0–255). */
  rgb?: { r: number; g: number; b: number };
  /** HSL triple from aura-color-picker (h: 0–360, s/l: 0–100). */
  hsl?: { h: number; s: number; l: number };
  /** OKLCH triple from aura-color-picker (l: 0–1, c: 0–0.4, h: 0–360). */
  oklch?: { l: number; c: number; h: number };
  /** Alpha channel 0–1 (aura-color-picker). */
  alpha?: number;
  /** Current content as HTML (aura-editor). */
  html?: string;
  /** Current content as plain text (aura-editor). */
  text?: string;
}

/** Detail for `aura:copy` (copy-to-clipboard affordances, aura-code). */
export interface AuraCopyDetail {
  /** The text that was copied to the clipboard. */
  text: string;
}

/** Detail for `aura:dialog-close`. */
export interface AuraDialogCloseDetail {
  /** The dialog element that closed. */
  dialog: HTMLElement;
}

/** Detail for `aura:dialog-open`. */
export interface AuraDialogOpenDetail {
  /** The dialog element that opened. */
  dialog: HTMLElement;
  /** The element that triggered the open (null when opened programmatically). */
  opener: HTMLElement | null;
}

/** Detail for `aura:footer-revealed` (aura-footer intersection reveal/conceal). */
export interface AuraFooterRevealedDetail {
  /** True when the footer enters the viewport; false when it leaves. */
  revealed: boolean;
}

/** Detail for `aura:form-change` (aura-form dependent-field engine). */
export interface AuraFormChangeDetail {
  /** Number of managed fields after the change. */
  managed: number;
}

/** Detail for `aura:menu-select` (context/dropdown menus). */
export interface AuraMenuSelectDetail {
  /** The item's value attribute (falls back to its label). */
  value: string;
  /** The item's visible label text. */
  label: string;
  /** Checked state for checkable items (always present; false when not checkable). */
  checked: boolean;
  /** The selected menu item element. */
  item: HTMLElement;
  /** The item's data-aura-action, if any. */
  action: string | null;
}

/** Detail for `aura:nav-panel-open` (aura-nav-header dropdown panels). */
export interface AuraNavPanelOpenDetail {
  /** The trigger button that opened the panel. */
  trigger: HTMLElement;
}

/** Detail for `aura:tab-change` (aura-tabs). */
export interface AuraTabChangeDetail {
  /** The newly-selected tab. */
  tab: HTMLElement;
  /** The previously-selected tab (null on initial sync). */
  previous: HTMLElement | null;
}

/**
 * Typed map of every Aura custom event NAME → event object, generated from
 * the literal `new CustomEvent(…)` dispatch sites in js/. Extend this
 * interface (declaration merging) to add your own events.
 */
export interface AuraEventMap {
  "aura:change": CustomEvent<AuraChangeDetail>;
  "aura:copy": CustomEvent<AuraCopyDetail>;
  "aura:dialog-close": CustomEvent<AuraDialogCloseDetail>;
  "aura:dialog-open": CustomEvent<AuraDialogOpenDetail>;
  "aura:dirty": CustomEvent<undefined>;
  "aura:footer-revealed": CustomEvent<AuraFooterRevealedDetail>;
  "aura:form-change": CustomEvent<AuraFormChangeDetail>;
  "aura:menu-select": CustomEvent<AuraMenuSelectDetail>;
  "aura:nav-panel-open": CustomEvent<AuraNavPanelOpenDetail>;
  "aura:tab-change": CustomEvent<AuraTabChangeDetail>;
}

/** Union of all built-in Aura event names — `keyof AuraEventMap`. */
export type AuraEventName = keyof AuraEventMap;

/** A partial map of event listeners for any Aura element. */
export type AuraEventHandlers = {
  [K in keyof AuraEventMap]?: (event: AuraEventMap[K]) => void;
} & {
  /** Consumer-dispatched / future events: any other name is accepted. */
  [key: string]: ((event: CustomEvent) => void) | undefined;
};

/* --------------------------------------------------------------------------
 * Base props shared by every Aura wrapper component
 * ----------------------------------------------------------------------- */

/**
 * Props accepted by every `createAuraComponent` wrapper.
 *
 * Standard DOM/React props (id, style, onClick, …) come from
 * `HTMLAttributes`; `data-*` / `aria-*` attributes pass through via the
 * template-literal index signatures (#422) — unknown props such as typos
 * (`varient=`) are excess-property errors.
 *
 * React's native `onChange` is omitted: on Aura form controls `onChange`
 * is the controlled-bridge sugar `(value, event) => void` (#419), declared
 * per-element below; other Aura elements dispatch no change events (use
 * the `events` map for custom events).
 */
export interface AuraComponentProps extends PropsWithChildren<Omit<HTMLAttributes<HTMLElement>, "onChange">> {
  /**
   * Map of custom-event names to handlers. Listener identity is stable
   * across re-renders (handlers are read through a ref); listeners are
   * (re)attached only when the set of event NAMES changes.
   *
   * @example
   * ```tsx
   * <AuraEditor events={{ "aura:change": (e) => setHtml(e.detail.html) }} />
   * ```
   */
  events?: AuraEventHandlers;

  /** Standard HTML `class` attribute (alias accepted by all wrappers). */
  class?: string;

  /** `data-*` attributes pass through to the underlying element. */
  [key: `data-${string}`]: string | number | boolean | undefined;
  /** `aria-*` attributes pass through to the underlying element. */
  [key: `aria-${string}`]: string | number | boolean | undefined;
}

/* --------------------------------------------------------------------------
 * createAuraComponent factory
 * ----------------------------------------------------------------------- */

/** Props for a wrapper created by `createAuraComponent`. */
export type AuraWrappedProps<T extends HTMLElement = HTMLElement> =
  AuraComponentProps & RefAttributes<T>;

/**
 * Wraps an Aura custom element as a React component.
 *
 * - Forwards refs to the underlying DOM element.
 * - Reflects boolean props as attribute presence/absence on EVERY React
 *   version (React ≤18 would otherwise serialize `checked={false}` into a
 *   present `checked="false"` attribute — #417).
 * - Wires the `events` map with stable listeners (no per-render churn).
 * - Spreads all other props as attributes.
 *
 * @example
 * ```tsx
 * import { createAuraComponent } from "@aura-design/core/bindings/react";
 *
 * // Wrap a custom element that isn't yet in the built-in set:
 * const AuraMyWidget = createAuraComponent<HTMLElement>("aura-my-widget");
 * // <AuraMyWidget ref={ref} mode="advanced" />
 * ```
 */
export function createAuraComponent<T extends HTMLElement = HTMLElement>(
  tagName: string
): ForwardRefExoticComponent<AuraWrappedProps<T>>;

/* --------------------------------------------------------------------------
 * Typed wrappers — one per registry element (generated — 55 tags)
 * ----------------------------------------------------------------------- */

/** `<aura-app>` — top-level application scope — mirrors theme/color attributes onto the subtree. */
export interface AuraAppProps extends Omit<AuraComponentProps, "events"> {
  /** Dark/light MODE applied to the subtree (data-aura-theme): dark / light / auto. NOT the named-theme axis — use `theme-name` for that (#570/#574/#597). */
  theme?: "dark" | "light" | "auto";
  /** Named theme applied to the subtree (data-aura-theme-name) — the axis that carries the colour tokens (e.g. "warm-dusk"). Orthogonal to `theme` (the dark/light mode axis) (#597). */
  "theme-name"?: string;
  /** Primary accent hex override — sets `--aura-primary` on the scope (e.g. "#7654f5"). */
  primary?: string;
  /** Secondary accent hex override — sets `--aura-secondary` on the scope. */
  secondary?: string;
  /** Wallpaper treatment: a CSS url(), gradient, "aurora", or "none". */
  wallpaper?: string;
}
export declare const AuraApp: ForwardRefExoticComponent<
  AuraAppProps & RefAttributes<HTMLElement>
>;

/** `<aura-avatar>` — user/entity avatar with image, initials, and status dot. */
export interface AuraAvatarProps extends Omit<AuraComponentProps, "events"> {
  /** Avatar image URL. */
  src?: string;
  /** Alt text for the avatar image. */
  alt?: string;
  /** Fallback initials when no image is set. */
  initials?: string;
  /** Avatar size keyword. */
  size?: "sm" | "lg";
  /** Presence status keyword (renders the status dot). */
  status?: "online" | "busy" | "away" | "offline";
}
export declare const AuraAvatar: ForwardRefExoticComponent<
  AuraAvatarProps & RefAttributes<HTMLElement>
>;

/** `<aura-badge>` — non-interactive semantic label — status chip for conveying state, category, or count inline with other content. */
export interface AuraBadgeProps extends Omit<AuraComponentProps, "events"> {
  /** Accessible label for the badge (visible only to screen readers when badge text is purely symbolic). */
  label?: string;
  /** Semantic colour variant. */
  variant?: "success" | "warning" | "danger" | "info" | "neutral";
}
export declare const AuraBadge: ForwardRefExoticComponent<
  AuraBadgeProps & RefAttributes<HTMLElement>
>;

/** `<aura-breadcrumb>` — breadcrumb navigation landmark — wrap <a> children for the trail; last item is aria-current="page" automatically. */
export interface AuraBreadcrumbProps extends Omit<AuraComponentProps, "events"> {
  /** Accessible label for the nav landmark (default: "Breadcrumb"). */
  label?: string;
}
export declare const AuraBreadcrumb: ForwardRefExoticComponent<
  AuraBreadcrumbProps & RefAttributes<HTMLElement>
>;

/** `<aura-button>` — action button with four visual variants (primary / secondary / ghost / danger) and optional icon. */
export interface AuraButtonProps extends Omit<AuraComponentProps, "events"> {
  /** Visual style. */
  variant?: "primary" | "secondary" | "ghost" | "danger";
  /** Named sprite icon rendered before the label. */
  icon?: string;
  /** Disables the button. */
  disabled?: boolean;
  /** HTML button type. */
  type?: "button" | "submit" | "reset";
  /** Loading state — shows a spinner and blocks interaction. */
  loading?: boolean;
  /** Icon-only mode — hides the label text for sighted users; children text still provides the accessible name. */
  "icon-only"?: boolean;
}
export declare const AuraButton: ForwardRefExoticComponent<
  AuraButtonProps & RefAttributes<HTMLElement>
>;

/** `<aura-card>` — elevated surface container with 0–5 depth levels; set interactive for a clickable card variant. */
export interface AuraCardProps extends Omit<AuraComponentProps, "events"> {
  /** Elevation depth: 0 = flush/borderless, 1–2 = subtle raised, 3–4 = floating, 5 = dialog-height. */
  elevation?: "0" | "1" | "2" | "3" | "4" | "5";
  /** Makes the entire card interactive (hover/focus states). */
  interactive?: boolean;
  /** Pointer-tracking sheen treatment. */
  tactile?: boolean;
}
export declare const AuraCard: ForwardRefExoticComponent<
  AuraCardProps & RefAttributes<HTMLElement>
>;

/** `<aura-checkbox>` — checkbox control backed by a native input — supports indeterminate state; `aura:change.detail` carries `{ checked, indeterminate, value }`. */
export interface AuraCheckboxProps extends AuraComponentProps {
  /** Checked state; presence of the attribute = checked (mirrors native checkbox). Passing this together with `onChange` makes the component CONTROLLED (#419): the wrapper re-asserts it after every change, so the DOM only moves when the app commits the new value. */
  checked?: boolean;
  /** Disables the control. */
  disabled?: boolean;
  /** Indeterminate (mixed) state — overrides the checked visual; cleared on next user toggle. */
  indeterminate?: boolean;
  /** Form value submitted when checked (defaults to "on" like a native checkbox). */
  value?: string;
  /** Form field name. */
  name?: string;
  /** Accessible name for icon-only / unlabeled usage. */
  label?: string;
  /**
   * Controlled-bridge change sugar (#419): called with the new
   * value (from the `aura:change` detail) and the raw event on
   * every user change. Works uncontrolled too (sugar only);
   * combined with `checked` it enables controlled semantics.
   */
  onChange?: (value: boolean, event: AuraEventMap["aura:change"]) => void;
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:change"?: (event: CustomEvent<AuraChangeDetail>) => void;
  };
}
export declare const AuraCheckbox: ForwardRefExoticComponent<
  AuraCheckboxProps & RefAttributes<HTMLElement>
>;

/** `<aura-chip>` — compact label / status pill for tags, counts, and inline state — supports semantic variants. */
export interface AuraChipProps extends Omit<AuraComponentProps, "events"> {
  /** Semantic colour variant. */
  variant?: "success" | "warning" | "danger" | "info" | "neutral";
}
export declare const AuraChip: ForwardRefExoticComponent<
  AuraChipProps & RefAttributes<HTMLElement>
>;

/** `<aura-code>` — inline / block code presentation with optional copy affordance. */
export interface AuraCodeProps extends AuraComponentProps {
  /** Block (multi-line) presentation. */
  block?: boolean;
  /** Language tag shown on block code. */
  lang?: string;
  /** Adds the copy-to-clipboard affordance. */
  copyable?: boolean;
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:copy"?: (event: CustomEvent<AuraCopyDetail>) => void;
  };
}
export declare const AuraCode: ForwardRefExoticComponent<
  AuraCodeProps & RefAttributes<HTMLElement>
>;

/** `<aura-color-picker>` — OKLCH-native color picker — outputs hex, rgb, hsl, or oklch; optional alpha slider and inline (always-open) mode. */
export interface AuraColorPickerProps extends AuraComponentProps {
  /** Current color value in the active serialization format. Passing this together with `onChange` makes the component CONTROLLED (#419): the wrapper re-asserts it after every change, so the DOM only moves when the app commits the new value. */
  value?: string;
  /** Form field name. */
  name?: string;
  /** Serialization format emitted in the `aura:change` value field (default: hex). */
  format?: "hex" | "rgb" | "hsl" | "oklch";
  /** Enables the alpha channel — shows the alpha slider and appends alpha to the output. */
  alpha?: boolean;
  /** Inline (always-open) presentation. */
  inline?: boolean;
  /** Disables the control. */
  disabled?: boolean;
  /**
   * Controlled-bridge change sugar (#419): called with the new
   * value (from the `aura:change` detail) and the raw event on
   * every user change. Works uncontrolled too (sugar only);
   * combined with `value` it enables controlled semantics.
   */
  onChange?: (value: string, event: AuraEventMap["aura:change"]) => void;
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:change"?: (event: CustomEvent<AuraChangeDetail>) => void;
  };
}
export declare const AuraColorPicker: ForwardRefExoticComponent<
  AuraColorPickerProps & RefAttributes<HTMLElement>
>;

/** `<aura-datepicker>` — calendar date picker (single or range). */
export interface AuraDatepickerProps extends AuraComponentProps {
  /** ISO date value in YYYY-MM-DD format; YYYY-MM-DD/YYYY-MM-DD in range mode. Passing this together with `onChange` makes the component CONTROLLED (#419): the wrapper re-asserts it after every change, so the DOM only moves when the app commits the new value. */
  value?: string;
  /** Form field name. */
  name?: string;
  /** Earliest selectable date in YYYY-MM-DD format. */
  min?: string;
  /** Latest selectable date in YYYY-MM-DD format. */
  max?: string;
  /** BCP-47 locale for month/weekday names (e.g. "en-GB", "fr-FR"). */
  locale?: string;
  /** 0 (Sunday) – 6 (Saturday); defaults to locale preference. */
  "first-day-of-week"?: string | number;
  /** Trigger placeholder text. */
  placeholder?: string;
  /** Disables the control. */
  disabled?: boolean;
  /** Range mode ("dual" or bare attribute). */
  range?: string | boolean;
  /**
   * Controlled-bridge change sugar (#419): called with the new
   * value (from the `aura:change` detail) and the raw event on
   * every user change. Works uncontrolled too (sugar only);
   * combined with `value` it enables controlled semantics.
   */
  onChange?: (value: string, event: AuraEventMap["aura:change"]) => void;
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:change"?: (event: CustomEvent<AuraChangeDetail>) => void;
  };
}
export declare const AuraDatepicker: ForwardRefExoticComponent<
  AuraDatepickerProps & RefAttributes<HTMLElement>
>;

/** `<aura-dialog>` — modal dialog with focus-trap and scrim (add non-modal for a lightweight popover-like variant). */
export interface AuraDialogProps extends AuraComponentProps {
  /** Renders as a non-modal (no scrim, no focus trap). */
  "non-modal"?: boolean;
  /** Controls open/closed state; prefer useAuraDialog or Aura.dialog helpers to manage focus correctly. */
  open?: boolean;
  /** Dialog title — synthesizes .aura-dialog__header/__title chrome automatically. */
  title?: string;
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:dialog-open"?: (event: CustomEvent<AuraDialogOpenDetail>) => void;
    "aura:dialog-close"?: (event: CustomEvent<AuraDialogCloseDetail>) => void;
  };
}
export declare const AuraDialog: ForwardRefExoticComponent<
  AuraDialogProps & RefAttributes<HTMLElement>
>;

/** `<aura-editor>` — rich-text / HTML editor backed by a contenteditable — mirrors content into a hidden form input. */
export interface AuraEditorProps extends AuraComponentProps {
  /** Form field name (mirrors content into a hidden input). */
  name?: string;
  /** Placeholder shown while empty. */
  placeholder?: string;
  /** Plain mode — no toolbar chrome. */
  plain?: boolean;
  /** Disables the editor (non-editable + aria-disabled). */
  disabled?: boolean;
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:change"?: (event: CustomEvent<AuraChangeDetail>) => void;
  };
}
export declare const AuraEditor: ForwardRefExoticComponent<
  AuraEditorProps & RefAttributes<HTMLElement>
>;

/** `<aura-empty-state>` — empty-state panel with icon, title, and actions row — children become the actions (use `<aura-button>` children). */
export interface AuraEmptyStateProps extends Omit<AuraComponentProps, "events"> {
  /** Named sprite icon shown above the title. */
  icon?: string;
  /** Headline text. */
  title?: string;
}
export declare const AuraEmptyState: ForwardRefExoticComponent<
  AuraEmptyStateProps & RefAttributes<HTMLElement>
>;

/** `<aura-field>` — form field wrapper — auto-wires the label `for`/`id` association, stacks hint and error text below the control. */
export interface AuraFieldProps extends Omit<AuraComponentProps, "events"> {
  /** The label text (auto-linked to the first form control child via `for`/`id`). */
  label?: string;
  /** Helper text shown below the field in the resting state. */
  hint?: string;
  /** Error message shown below the field; overrides hint when set. */
  error?: string;
  /** Row layout — places label and control side-by-side instead of stacked. */
  layout?: "row";
  /** Marks the field as required (adds the required indicator to the label). */
  required?: boolean;
  /** Marks the field as disabled (propagates disabled to the slotted control). */
  disabled?: boolean;
}
export declare const AuraField: ForwardRefExoticComponent<
  AuraFieldProps & RefAttributes<HTMLElement>
>;

/** `<aura-fmb-column>` — fixed left-rail column that houses the three Floating Menu Buttons (nav, sidebar, footer) when all stashed — provides the 96px backdrop rail and emits page-offset tokens; place once near the top of <body>. */
export interface AuraFmbColumnProps extends Omit<AuraComponentProps, "events"> {
  /** Minimum number of chrome elements that must be stashed to activate the column (default: all present elements). */
  "data-fmb-column-min"?: string | number;
}
export declare const AuraFmbColumn: ForwardRefExoticComponent<
  AuraFmbColumnProps & RefAttributes<HTMLElement>
>;

/** `<aura-footer>` — page footer chrome — IntersectionObserver reveal animation; add `data-aura-footer` to any element to trigger footer reveal when it enters the viewport. */
export interface AuraFooterProps extends AuraComponentProps {
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:footer-revealed"?: (event: CustomEvent<AuraFooterRevealedDetail>) => void;
  };
}
export declare const AuraFooter: ForwardRefExoticComponent<
  AuraFooterProps & RefAttributes<HTMLElement>
>;

/** `<aura-form>` — form orchestration wrapper — adds dependent-field enabling/disabling, dirty-state tracking, and an `aura:form-change` aggregate on top of a standard <form> element. */
export interface AuraFormProps extends AuraComponentProps {
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:form-change"?: (event: CustomEvent<AuraFormChangeDetail>) => void;
    "aura:dirty"?: (event: CustomEvent<undefined>) => void;
  };
}
export declare const AuraForm: ForwardRefExoticComponent<
  AuraFormProps & RefAttributes<HTMLElement>
>;

/** `<aura-grid>` — grid layout primitive — auto-fitting CSS grid with a token-driven gap; children fill equal columns. */
export interface AuraGridProps extends Omit<AuraComponentProps, "events"> {
  /** Token-step gap between grid cells (default: 4 = 1rem). */
  gap?: "0" | "1" | "2" | "3" | "4" | "5" | "6" | "7";
}
export declare const AuraGrid: ForwardRefExoticComponent<
  AuraGridProps & RefAttributes<HTMLElement>
>;

/** `<aura-icon>` — named sprite icon — pass the icon name as children text content: <AuraIcon>settings</AuraIcon>. */
export interface AuraIconProps extends Omit<AuraComponentProps, "events"> {
  /** Accessible name (img semantics) when not decorative; omit for decorative icons. */
  label?: string;
  /** Icon size keyword. */
  size?: "sm" | "md" | "lg" | "xl";
  /** Visual colour variant. */
  variant?: "muted" | "subtle" | "primary" | "secondary";
}
export declare const AuraIcon: ForwardRefExoticComponent<
  AuraIconProps & RefAttributes<HTMLElement>
>;

/** `<aura-image>` — framed image with caption, ratio, and expandable lightbox. */
export interface AuraImageProps extends Omit<AuraComponentProps, "events"> {
  /** Image URL. */
  src?: string;
  /** Alt text. */
  alt?: string;
  /** Caption rendered under the image. */
  caption?: string;
  /** Aspect ratio (e.g. "16/9"). */
  ratio?: string;
  /** object-fit keyword. */
  fit?: string;
  /** Native loading hint (defers off-screen load when lazy). */
  loading?: "lazy" | "eager";
  /** Click-to-expand lightbox affordance. */
  expandable?: boolean;
}
export declare const AuraImage: ForwardRefExoticComponent<
  AuraImageProps & RefAttributes<HTMLElement>
>;

/** `<aura-link>` — styled anchor with proximity glow — use instead of a plain <a> for inline text links; supports ghost (default) and primary variants. */
export interface AuraLinkProps extends Omit<AuraComponentProps, "events"> {
  /** Link destination. */
  href?: string;
  /** Link target. */
  target?: string;
  /** Visual style variant. */
  variant?: "ghost" | "primary";
}
export declare const AuraLink: ForwardRefExoticComponent<
  AuraLinkProps & RefAttributes<HTMLElement>
>;

/** `<aura-menu>` — dropdown / context-menu container — wrap <aura-menu-item> children; trigger with `data-aura-menu="#id"` on any element. */
export interface AuraMenuProps extends AuraComponentProps {
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:menu-select"?: (event: CustomEvent<AuraMenuSelectDetail>) => void;
  };
}
export declare const AuraMenu: ForwardRefExoticComponent<
  AuraMenuProps & RefAttributes<HTMLElement>
>;

/** `<aura-menu-item>` — one row inside an <aura-menu> — pass data-aura-action for a string action token reported in AuraMenuSelectDetail.action. */
export interface AuraMenuItemProps extends Omit<AuraComponentProps, "events"> {
  /** Value reported by aura:menu-select (falls back to the label). */
  value?: string;
  /** Checked state for checkable items. */
  checked?: boolean;
  /** Disables the item. */
  disabled?: boolean;
}
export declare const AuraMenuItem: ForwardRefExoticComponent<
  AuraMenuItemProps & RefAttributes<HTMLElement>
>;

/** `<aura-nav-group>` — labelled group of <aura-nav-item> children inside a nav panel. */
export interface AuraNavGroupProps extends Omit<AuraComponentProps, "events"> {
  /** Visible group header label rendered above the group's items. */
  label?: string;
}
export declare const AuraNavGroup: ForwardRefExoticComponent<
  AuraNavGroupProps & RefAttributes<HTMLElement>
>;

/** `<aura-nav-header>` — application navigation header (banner landmark) — place as first child of <aura-shell>; children become the start-slot; <aura-nav-item> / <aura-nav-menu> populate the nav row. */
export interface AuraNavHeaderProps extends AuraComponentProps {
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:nav-panel-open"?: (event: CustomEvent<AuraNavPanelOpenDetail>) => void;
  };
}
export declare const AuraNavHeader: ForwardRefExoticComponent<
  AuraNavHeaderProps & RefAttributes<HTMLElement>
>;

/** `<aura-nav-item>` — one declarative nav row — <a> when href is present, <button> otherwise. */
export interface AuraNavItemProps extends Omit<AuraComponentProps, "events"> {
  /** Destination (renders an <a>). */
  href?: string;
  /** Named sprite icon. */
  icon?: string;
  /** Count badge. */
  badge?: string | number;
  /** Secondary description line. */
  description?: string;
  /** Tooltip text. */
  tooltip?: string;
  /** Marks the current page (aria-current). */
  current?: boolean;
  /** Places the item in the panel footer row. */
  footer?: boolean;
  /** Anchor target passthrough. */
  target?: string;
}
export declare const AuraNavItem: ForwardRefExoticComponent<
  AuraNavItemProps & RefAttributes<HTMLElement>
>;

/** `<aura-nav-menu>` — declarative nav dropdown — synthesizes the wired trigger + panel (#392). */
export interface AuraNavMenuProps extends Omit<AuraComponentProps, "events"> {
  /** Trigger label (also derives the panel id). */
  label?: string;
  /** Count badge on the trigger. */
  badge?: string | number;
  /** Adds the panel filter field. */
  searchable?: boolean;
  /** Mega-menu (multi-column) panel. */
  mega?: boolean;
  /** User-menu treatment (data-nav-user-menu). */
  user?: boolean;
  /** Marks the entry with the "new" treatment. */
  new?: boolean;
  /** Trigger tooltip text. */
  tooltip?: string;
  /** Explicit panel id (overrides the label-derived id). */
  "menu-id"?: string;
}
export declare const AuraNavMenu: ForwardRefExoticComponent<
  AuraNavMenuProps & RefAttributes<HTMLElement>
>;

/** `<aura-option>` — one option inside <aura-select> — text content is the display label; `value` is what gets submitted. */
export interface AuraOptionProps extends Omit<AuraComponentProps, "events"> {
  /** Submitted form value (falls back to the text content if omitted). */
  value?: string;
  /** Initially-selected state. */
  selected?: boolean;
  /** Disables the option. */
  disabled?: boolean;
}
export declare const AuraOption: ForwardRefExoticComponent<
  AuraOptionProps & RefAttributes<HTMLElement>
>;

/** `<aura-option-group>` — labelled option group inside <aura-select> — renders as an optgroup equivalent. */
export interface AuraOptionGroupProps extends Omit<AuraComponentProps, "events"> {
  /** Group header label shown above the grouped options. */
  label?: string;
}
export declare const AuraOptionGroup: ForwardRefExoticComponent<
  AuraOptionGroupProps & RefAttributes<HTMLElement>
>;

/** `<aura-pagination>` — declarative pagination navigation; manages aria-current="page" on the active link. */
export interface AuraPaginationProps extends Omit<AuraComponentProps, "events"> {
  /** The active page — href match or 1-based page number. */
  current?: string;
  /** Accessible label for the nav landmark (default: "Pagination"). */
  label?: string;
}
export declare const AuraPagination: ForwardRefExoticComponent<
  AuraPaginationProps & RefAttributes<HTMLElement>
>;

/** `<aura-panel>` — elevated surface container for grouped content — shares the card elevated-surface recipe (background, stroke, elevation modes). */
export interface AuraPanelProps extends Omit<AuraComponentProps, "events"> {
  /** Elevation depth (default inherits the surface token). */
  elevation?: "0" | "1" | "2" | "3" | "4" | "5";
}
export declare const AuraPanel: ForwardRefExoticComponent<
  AuraPanelProps & RefAttributes<HTMLElement>
>;

/** `<aura-progress>` — declarative progress bar with ARIA and --aura-progress-value wiring. */
export interface AuraProgressProps extends Omit<AuraComponentProps, "events"> {
  /** Current progress value (0–max; clamped). Required for determinate mode. */
  value?: string | number;
  /** Maximum value (default 100). */
  max?: string | number;
  /** Indeterminate (loading) mode — omits aria-valuenow. */
  indeterminate?: boolean;
  /** Semantic colour variant. */
  variant?: "success" | "warning" | "danger";
}
export declare const AuraProgress: ForwardRefExoticComponent<
  AuraProgressProps & RefAttributes<HTMLElement>
>;

/** `<aura-radio>` — radio button backed by a native input — group siblings with the same `name`. */
export interface AuraRadioProps extends AuraComponentProps {
  /** Checked state; mutually exclusive within the same `name` group. Passing this together with `onChange` makes the component CONTROLLED (#419): the wrapper re-asserts it after every change, so the DOM only moves when the app commits the new value. */
  checked?: boolean;
  /** Disables this radio button. */
  disabled?: boolean;
  /** Form value submitted when this button is the checked member of the group. */
  value?: string;
  /** Group identifier — all radios sharing a `name` are mutually exclusive and form a single field. */
  name?: string;
  /** Accessible name when the visible label is not a sibling text node. */
  label?: string;
  /**
   * Controlled-bridge change sugar (#419): called with the new
   * value (from the `aura:change` detail) and the raw event on
   * every user change. Works uncontrolled too (sugar only);
   * combined with `checked` it enables controlled semantics.
   */
  onChange?: (value: boolean, event: AuraEventMap["aura:change"]) => void;
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:change"?: (event: CustomEvent<AuraChangeDetail>) => void;
  };
}
export declare const AuraRadio: ForwardRefExoticComponent<
  AuraRadioProps & RefAttributes<HTMLElement>
>;

/** `<aura-range>` — slider / range input (single or dual-handle). */
export interface AuraRangeProps extends AuraComponentProps {
  /** Minimum value. */
  min?: string | number;
  /** Maximum value. */
  max?: string | number;
  /** Step increment. */
  step?: string | number;
  /** Disables the control. */
  disabled?: boolean;
  /** Form field name. */
  name?: string;
  /** Accessible name for unlabeled usage. */
  label?: string;
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:change"?: (event: CustomEvent<AuraChangeDetail>) => void;
  };
}

/**
 * (#557) Mode-specific controlled typing for `<aura-range>`.
 *
 * `aura-range` is statically single-mode unless the `range` boolean prop is
 * present, so the controlled `value`/`onChange` discriminate on it:
 * the single-mode arm hands back `number` (no narrowing or cast at the
 * common call site), the multi arm `number[]`.
 */
export type AuraRangeModeProps =
  | {
      /** Multi mode off (default) — value/onChange are scalar. */
      range?: false;
      value?: string | number;
      onChange?: (value: number, event: AuraEventMap["aura:change"]) => void;
    }
  | {
      /** Multi mode on — value/onChange are arrays. */
      range: true;
      value?: number[];
      onChange?: (value: number[], event: AuraEventMap["aura:change"]) => void;
    };
export declare const AuraRange: ForwardRefExoticComponent<
  AuraRangeProps & AuraRangeModeProps & RefAttributes<HTMLElement>
>;

/** `<aura-row>` — horizontal flex layout primitive — lays children in a row with a token-driven gap and optional alignment controls. */
export interface AuraRowProps extends Omit<AuraComponentProps, "events"> {
  /** Token-step gap between items (default: 4 = 1rem). */
  gap?: "0" | "1" | "2" | "3" | "4" | "5" | "6" | "7";
  /** Cross-axis alignment (align-items). */
  align?: "start" | "center" | "end" | "stretch";
  /** Main-axis justification (justify-content). */
  justify?: "start" | "center" | "end" | "between";
  /** Allows children to wrap to the next line. */
  wrap?: boolean;
}
export declare const AuraRow: ForwardRefExoticComponent<
  AuraRowProps & RefAttributes<HTMLElement>
>;

/** `<aura-select>` — select / dropdown control — supports single and multi-select; pairs with <aura-option> children. */
export interface AuraSelectProps extends AuraComponentProps {
  /** Form field name. */
  name?: string;
  /** Disables the control. */
  disabled?: boolean;
  /** Placeholder shown while empty. */
  placeholder?: string;
  /** Adds the option filter field. */
  searchable?: boolean;
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:change"?: (event: CustomEvent<AuraChangeDetail>) => void;
  };
}

/**
 * (#557) Mode-specific controlled typing for `<aura-select>`.
 *
 * `aura-select` is statically single-mode unless the `multiple` boolean prop is
 * present, so the controlled `value`/`onChange` discriminate on it:
 * the single-mode arm hands back `string` (no narrowing or cast at the
 * common call site), the multi arm `string[]`.
 */
export type AuraSelectModeProps =
  | {
      /** Multi mode off (default) — value/onChange are scalar. */
      multiple?: false;
      value?: string;
      onChange?: (value: string, event: AuraEventMap["aura:change"]) => void;
    }
  | {
      /** Multi mode on — value/onChange are arrays. */
      multiple: true;
      value?: string[];
      onChange?: (value: string[], event: AuraEventMap["aura:change"]) => void;
    };
export declare const AuraSelect: ForwardRefExoticComponent<
  AuraSelectProps & AuraSelectModeProps & RefAttributes<HTMLElement>
>;

/** `<aura-spacer>` — flexible spacing primitive — expands to push surrounding flex/inline content apart (declarative flex-grow gap). */
export declare const AuraSpacer: ForwardRefExoticComponent<
  Omit<AuraComponentProps, "events"> & RefAttributes<HTMLElement>
>;

/** `<aura-sparkline>` — inline sparkline element — synthesises SVG area+line from comma-separated values. */
export interface AuraSparklineProps extends Omit<AuraComponentProps, "events"> {
  /** Comma-separated numbers to plot (e.g. "24,22,25,16,18,10,6"). */
  values?: string;
  /** Accessible name; written as aria-label on the host. */
  label?: string;
  /** Boolean trend flag; adds data-trend="" on the host for CSS styling. */
  trend?: boolean;
}
export declare const AuraSparkline: ForwardRefExoticComponent<
  AuraSparklineProps & RefAttributes<HTMLElement>
>;

/** `<aura-split>` — resizable split panes — direct children become the panes; a drag handle is injected between them. */
export interface AuraSplitProps extends AuraComponentProps {
  /** Split axis: horizontal (side-by-side) or vertical (stacked). */
  orientation?: "horizontal" | "vertical";
  /** localStorage key — stores the pane size ratios between page loads. */
  persist?: string;
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:change"?: (event: CustomEvent<AuraChangeDetail>) => void;
  };
}
export declare const AuraSplit: ForwardRefExoticComponent<
  AuraSplitProps & RefAttributes<HTMLElement>
>;

/** `<aura-stack>` — vertical flex layout primitive — stacks children in a column with a token-driven gap and optional alignment controls. */
export interface AuraStackProps extends Omit<AuraComponentProps, "events"> {
  /** Token-step gap between items (default: 4 = 1rem). */
  gap?: "0" | "1" | "2" | "3" | "4" | "5" | "6" | "7";
  /** Cross-axis alignment (align-items). */
  align?: "start" | "center" | "end" | "stretch";
  /** Main-axis justification (justify-content). */
  justify?: "start" | "center" | "end" | "between";
}
export declare const AuraStack: ForwardRefExoticComponent<
  AuraStackProps & RefAttributes<HTMLElement>
>;

/** `<aura-stepper>` — numeric stepper input with +/− buttons; respects min, max, and step constraints. */
export interface AuraStepperProps extends AuraComponentProps {
  /** Current value (clamped to min–max on commit). Passing this together with `onChange` makes the component CONTROLLED (#419): the wrapper re-asserts it after every change, so the DOM only moves when the app commits the new value. */
  value?: string | number;
  /** Minimum value (default 0). */
  min?: string | number;
  /** Maximum value. */
  max?: string | number;
  /** Step increment per +/− click (default 1). */
  step?: string | number;
  /** Disables the control. */
  disabled?: boolean;
  /** Form field name. */
  name?: string;
  /**
   * Controlled-bridge change sugar (#419): called with the new
   * value (from the `aura:change` detail) and the raw event on
   * every user change. Works uncontrolled too (sugar only);
   * combined with `value` it enables controlled semantics.
   */
  onChange?: (value: number, event: AuraEventMap["aura:change"]) => void;
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:change"?: (event: CustomEvent<AuraChangeDetail>) => void;
  };
}
export declare const AuraStepper: ForwardRefExoticComponent<
  AuraStepperProps & RefAttributes<HTMLElement>
>;

/** `<aura-steps>` — declarative step indicator; renders from authored <li> children or data-steps JSON. */
export interface AuraStepsProps extends Omit<AuraComponentProps, "events"> {
  /** JSON array of { label, state } step objects (codegen mode). state: "complete" | "current" | "upcoming" | "pending" (pending → upcoming). */
  "data-steps"?: string;
}
export declare const AuraSteps: ForwardRefExoticComponent<
  AuraStepsProps & RefAttributes<HTMLElement>
>;

/** `<aura-switch>` — toggle switch control — use instead of a checkbox for binary on/off settings (not for yes/no form answers); emits `aura:change.detail.checked`. */
export interface AuraSwitchProps extends AuraComponentProps {
  /** Whether the switch is on (reflects as a boolean property too). Passing this together with `onChange` makes the component CONTROLLED (#419): the wrapper re-asserts it after every change, so the DOM only moves when the app commits the new value. */
  checked?: boolean;
  /** Disables the switch. */
  disabled?: boolean;
  /** Accessible name — required when the visible label is not a sibling text node. */
  label?: string;
  /** Form field name (submitted like a checkbox — absent when off, value when on). */
  name?: string;
  /** Form value submitted when on (default "on"). */
  value?: string;
  /**
   * Controlled-bridge change sugar (#419): called with the new
   * value (from the `aura:change` detail) and the raw event on
   * every user change. Works uncontrolled too (sugar only);
   * combined with `checked` it enables controlled semantics.
   */
  onChange?: (value: boolean, event: AuraEventMap["aura:change"]) => void;
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:change"?: (event: CustomEvent<AuraChangeDetail>) => void;
  };
}
export declare const AuraSwitch: ForwardRefExoticComponent<
  AuraSwitchProps & RefAttributes<HTMLElement>
>;

/** `<aura-tab>` — one tab button inside <aura-tablist> — the text content is the tab label. */
export interface AuraTabProps extends Omit<AuraComponentProps, "events"> {
  /** Disables this tab (prevents selection). */
  disabled?: boolean;
}
export declare const AuraTab: ForwardRefExoticComponent<
  AuraTabProps & RefAttributes<HTMLElement>
>;

/** `<aura-tablist>` — tab strip — the direct child of <aura-tabs> that contains <aura-tab> items. */
export declare const AuraTablist: ForwardRefExoticComponent<
  Omit<AuraComponentProps, "events"> & RefAttributes<HTMLElement>
>;

/** `<aura-tabpanel>` — one content panel inside <aura-tabs> — positionally paired with the sibling <aura-tab> (first tab → first panel). */
export declare const AuraTabpanel: ForwardRefExoticComponent<
  Omit<AuraComponentProps, "events"> & RefAttributes<HTMLElement>
>;

/** `<aura-tabs>` — tabbed interface — structure: <aura-tablist> with <aura-tab> children + matching <aura-tabpanel> children; tab and panel are paired by index. */
export interface AuraTabsProps extends AuraComponentProps {
  /** 0-based index of the initially-selected tab (default 0). */
  selected?: string | number;
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:tab-change"?: (event: CustomEvent<AuraTabChangeDetail>) => void;
  };
}
export declare const AuraTabs: ForwardRefExoticComponent<
  AuraTabsProps & RefAttributes<HTMLElement>
>;

/** `<aura-tag-input>` — multi-tag text input — type and press Enter to add tags; `aura:change.detail.tags` is the live string[] array. */
export interface AuraTagInputProps extends AuraComponentProps {
  /** Initial tags as a comma-separated list (e.g. "react, vue, svelte"). Passing this together with `onChange` makes the component CONTROLLED (#419): the wrapper re-asserts it after every change, so the DOM only moves when the app commits the new value. */
  value?: string | string[];
  /** Form field name — one hidden `<input>` per tag is submitted with the form. */
  name?: string;
  /** Disables the control. */
  disabled?: boolean;
  /** Placeholder shown while the tag list is empty. */
  placeholder?: string;
  /**
   * Controlled-bridge change sugar (#419): called with the new
   * value (from the `aura:change` detail) and the raw event on
   * every user change. Works uncontrolled too (sugar only);
   * combined with `value` it enables controlled semantics.
   */
  onChange?: (value: string[], event: AuraEventMap["aura:change"]) => void;
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:change"?: (event: CustomEvent<AuraChangeDetail>) => void;
  };
}
export declare const AuraTagInput: ForwardRefExoticComponent<
  AuraTagInputProps & RefAttributes<HTMLElement>
>;

/** `<aura-theme-select>` — declarative named-theme switcher. */
export interface AuraThemeSelectProps extends AuraComponentProps {
  /** CSS selector for the theme apply target (defaults to <aura-app>). */
  scope?: string;
  /** Accessible name for the inner select. */
  label?: string;
  /** Skips reading/writing the choice to localStorage. */
  "no-persist"?: boolean;
  /** Disables the control. */
  disabled?: boolean;
  /** The active named theme. */
  value?: string;
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:change"?: (event: CustomEvent<AuraChangeDetail>) => void;
  };
}
export declare const AuraThemeSelect: ForwardRefExoticComponent<
  AuraThemeSelectProps & RefAttributes<HTMLElement>
>;

/** `<aura-theme-toggle>` — dark/light mode toggle button — reads and writes the resolved data-aura-theme on <html>. */
export interface AuraThemeToggleProps extends Omit<AuraComponentProps, "events"> {
  /** Disables the toggle. */
  disabled?: boolean;
  /** Skips persisting the choice to localStorage. */
  "no-persist"?: boolean;
}
export declare const AuraThemeToggle: ForwardRefExoticComponent<
  AuraThemeToggleProps & RefAttributes<HTMLElement>
>;

/** `<aura-timepicker>` — segmented time picker (single or range). */
export interface AuraTimepickerProps extends AuraComponentProps {
  /** Current time value in HH:MM (or HH:MM:SS with-seconds) format; HH:MM–HH:MM in range mode. Passing this together with `onChange` makes the component CONTROLLED (#419): the wrapper re-asserts it after every change, so the DOM only moves when the app commits the new value. */
  value?: string;
  /** Form field name. */
  name?: string;
  /** Force 12- or 24-hour display; defaults to locale preference. */
  "hour-cycle"?: "12" | "24";
  /** Minute step increment (e.g. 15 = :00/:15/:30/:45). */
  step?: string | number;
  /** Earliest selectable time in HH:MM format. */
  min?: string;
  /** Latest selectable time in HH:MM format. */
  max?: string;
  /** Adds the seconds segment. */
  "with-seconds"?: boolean;
  /** Disables the control. */
  disabled?: boolean;
  /** Read-only presentation. */
  readonly?: boolean;
  /** Marks the control as required. */
  required?: boolean;
  /** Visible format hint text. */
  "format-hint"?: string;
  /** Inline (always-open) presentation. */
  inline?: boolean;
  /** Range (start–end) mode. */
  range?: string | boolean;
  /**
   * Controlled-bridge change sugar (#419): called with the new
   * value (from the `aura:change` detail) and the raw event on
   * every user change. Works uncontrolled too (sugar only);
   * combined with `value` it enables controlled semantics.
   */
  onChange?: (value: string, event: AuraEventMap["aura:change"]) => void;
  /**
   * Typed handlers for the events this element dispatches. This map
   * is CLOSED (#476): only the names below type-check, so a typo
   * such as `"aura:chnge"` is an excess-property error rather than
   * silently accepted via an inherited index signature. To attach
   * a consumer-defined / future event, widen explicitly with the
   * `AuraEventHandlers` escape hatch, e.g.
   * `events={{ ...handlers } as AuraEventHandlers}`.
   */
  events?: {
    "aura:change"?: (event: CustomEvent<AuraChangeDetail>) => void;
  };
}
export declare const AuraTimepicker: ForwardRefExoticComponent<
  AuraTimepickerProps & RefAttributes<HTMLElement>
>;

/** `<aura-toolbar>` — horizontal action-bar surface grouping buttons and controls with a token-driven gap — shares the elevated-surface recipe. */
export interface AuraToolbarProps extends Omit<AuraComponentProps, "events"> {
  /** Token-step gap between toolbar items (default: 2). */
  gap?: "0" | "1" | "2" | "3" | "4" | "5" | "6" | "7";
}
export declare const AuraToolbar: ForwardRefExoticComponent<
  AuraToolbarProps & RefAttributes<HTMLElement>
>;

/** `<aura-tooltip>` — CSS-only hover/focus tooltip — reveals a positioned label bubble on hover or keyboard focus via ::after, no JavaScript. */
export interface AuraTooltipProps extends Omit<AuraComponentProps, "events"> {
  /** The tooltip label shown in the bubble (rendered via CSS attr(text)). */
  text?: string;
}
export declare const AuraTooltip: ForwardRefExoticComponent<
  AuraTooltipProps & RefAttributes<HTMLElement>
>;

/** `<aura-video>` — framed video player with poster, caption, and configurable aspect ratio. */
export interface AuraVideoProps extends Omit<AuraComponentProps, "events"> {
  /** Video URL. */
  src?: string;
  /** Poster image URL. */
  poster?: string;
  /** Caption rendered under the player. */
  caption?: string;
  /** Aspect ratio (e.g. "16/9"). */
  ratio?: string;
  /** Native lazy-load hint. */
  loading?: "lazy" | "eager";
}
export declare const AuraVideo: ForwardRefExoticComponent<
  AuraVideoProps & RefAttributes<HTMLElement>
>;
